# 01 目录结构说明

> 同步日期：2026-05-25  
> 分析范围：当前工作区文件树、GraphRAG 迁移后状态。

## 当前目录总览

```text
小王一号/
├── novel_system/          # 核心后端包，已全面迁移至 GraphRAG 架构
├── templates/             # Jinja2 页面模板 (dashboard.html)
├── static/                # 静态资源 (app.js, styles.css, js/ 模块)
├── tests/                 # pytest 测试套件，包含 graphrag_app/ 专项测试
├── scripts/               # 运维与缓存目录，已清理旧评测脚本
├── tasks/                 # 历史 PRD 目录，当前为空
├── docs/                  # 项目架构、迁移、审计文档
├── data/                  # 运行数据、GraphRAG Workspace (books/)、上传文本
├── .agents/               # 项目级 agent/skill 配置
├── .claude/               # Claude 本地配置
├── .codex/                # Codex hook/config
├── start_server.py        # Uvicorn 启动脚本
├── pyproject.toml         # pytest 配置
├── requirements.txt       # Python 依赖
├── README.md              # 项目说明
├── AGENTS.md              # AI 协作与开发规约
└── 《诛仙》_qinkan.net.txt # 根目录保留的测试素材
```

## 目录状态表

| 路径 | 当前状态 | 当前用途 | 备注 |
|---|---|---|---|
| `novel_system/` | 存在 | FastAPI、GraphRAG 适配层、重构后的索引包 | 核心代码区 |
| `templates/` | 存在 | `dashboard.html` | 前端模板 |
| `static/` | 活跃 | 存放样式表 (`css/`)、JS 模块 (`js/`，入口 `main.js`)；`app.js` 为遗留死代码 | 前端静态资源 |
| `tests/` | 活跃 | GraphRAG 专项测试 + 业务回归测试 | 181 个测试用例 |
| `scripts/` | 仅剩缓存 | 旧评测脚本已物理删除 | 清理完成 |
| `tasks/` | 为空 | 历史任务已清理 | 待新任务填入 |
| `docs/` | 活跃 | 正在更新为 GraphRAG 架构说明 | 正在同步 |
| `data/books/` | 活跃 | 包含每本书的 GraphRAG Workspace (input/output/settings) | 关键数据区 |
| `data/uploads/` | 存在 | 上传的原始小说文本 | |
| `data/vectors/` | 废弃 | 已由 GraphRAG 内部向量管理替代，待清理 | |
| `.agents/` | 存在 | 包含 GraphRAG 迁移相关的 User Stories | |

## 核心后端包说明 (`novel_system/`)

| 路径 | 当前用途 | 备注 |
|---|---|---|
| `graphrag_app/` | **GraphRAG 适配层**：包含 Workspace、InputBuilder、QueryEngine、IndexRunner、AnswerAdapter、Projectors 等 | **本次迁移核心新增** |
| `api.py` | FastAPI 路由，已适配 GraphRAG 异步查询模式 | |
| `service.py` | 服务工厂，返回注入了 GraphRAG 组件的 `GraphService` | |
| `services/` | 业务 Mixin：`qa.py` (GraphRAG 查询), `indexing.py` (GraphRAG 流水线) | 已重构 |
| `indexing/` | **重构后的索引子包**：包含 repository、parser、scene 等基础解析组件 | 已取代旧 `indexing.py` |
| `models.py` | Pydantic 模型，已新增 GraphRAG 相关请求/响应模型 | |
| `config.py` | 配置管理，新增 GraphRAG 相关环境变量映射 | |
| `llm.py` | LLM 客户端，支持 MiniMax 和 LiteLLM 路由 | |
| `graph_service.py`| 交互图谱服务，现从 GraphRAG Parquet 表投影数据 | |
| `retrieval.py` | **Legacy Facade**：保留部分类型定义，主链路已不再使用 | 待彻底清理 |
| `search/` | **Legacy Package**：包含旧版搜索编排，主链路已不再使用 | 待彻底清理 |
| `vector_store/` | **Legacy Package**：包含旧版 FAISS 向量库，主链路已不再使用 | 待彻底清理 |

## 已不存在或已清理的旧描述

| 路径 | 当前状态 | 备注 |
|---|---|---|
| `novel_system/indexing.py` | **已删除** | 已由 `novel_system/indexing/` 子包替代 |
| `eval_runner_template.py` | **已删除** | 旧评测模板已物理删除 |
| `show_trace.py` | **已删除** | 旧调试脚本已物理删除 |
| `fanren_eval_cases_v1.jsonl` | **已删除** | 旧评测数据已物理删除 |
| `scripts/*.py` | **已删除** | 所有旧评测脚本已物理删除 |
| `novel_system/legacy/` | **已删除** | 遗留代码已彻底清理 |

## 仍需确认的目录和文件

- 待确认：`data/vectors/` 目录下的旧 FAISS 索引以及 `novel_system/vector_store/`、`novel_system/search/`、`novel_system/retrieval.py` 是否可以物理删除（主链路已完全不依赖）。
- 已确认：根目录 `《诛仙》_qinkan.net.txt` 已被复制移入 `data/uploads/`，根目录下的副本可以被物理删除。
