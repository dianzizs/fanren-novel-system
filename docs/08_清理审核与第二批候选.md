# 08 清理审核与第二批候选

> 生成日期：2026-05-23  
> 审核目标：按当前真实主路线做激进瘦身，不为旧方案、备用方案、一次性脚本和历史文档保留位置。  
> 主路线定义：`templates/dashboard.html` + `static/app.js` -> `novel_system/api.py:create_app()` -> `novel_system/service.py:create_service()` -> `GraphService` -> `services/*` -> `indexing.py` / `retrieval.py` / `search/orchestrator.py` / `validator.py` / `llm.py`。

## 0. 关键前提

本次审核没有找到用户提到的 `docs/07_冗余文件清理记录.md`。当前 `docs/` 下实际存在：

- `docs/00_项目总览.md`
- `docs/01_目录结构说明.md`
- `docs/02_技术栈与启动流程.md`
- `docs/03_核心模块初步说明.md`
- `docs/04_文件用途审计表.md`
- `docs/05_疑似冗余文件清单.md`
- `docs/06_项目接管审查报告.md`
- `docs/project_takeover_analysis_framework.md`

因此，“第一轮清理安全性评估”以当前工作区真实状态为准，主要依据：

- `git diff --name-status`
- `git status --short --ignored`
- `rg --files`
- `rg -n` 引用搜索
- 已执行的轻量验证命令

已执行验证：

```bash
conda run -n chaishu python -m pytest tests/test_claude_skill_router.py
conda run -n chaishu python -m pytest tests/test_retrieval_metrics.py
```

结果：

- `tests/test_claude_skill_router.py`: 9 passed
- `tests/test_retrieval_metrics.py`: 17 passed

结论：`07` 记录文件缺失是文档链路问题，不是运行链路问题。当前没有证据表明第一轮删除破坏主流程。

---

# 1. 第一轮清理安全性评估

## 1.1 实际删除了哪些文件

根据 `git diff --name-status`，第一轮实际删除项是：

| 路径 | 类型 | 删除判断 |
|---|---|---|
| `docs/baseline/config.md` | 旧基线文档 | 基本安全 |
| `docs/baseline/modules.md` | 旧基线文档 | 基本安全 |
| `docs/superpowers/plans/2026-04-12-book-import-flow-plan.md` | 历史实施计划 | 基本安全 |
| `docs/superpowers/plans/2026-04-12-book-management-plan.md` | 历史实施计划 | 基本安全 |
| `docs/superpowers/plans/2026-04-13-claude-skill-routing.md` | 历史实施计划 | 基本安全 |
| `docs/superpowers/plans/2026-04-14-p0-fixes-plan.md` | 历史实施计划 | 基本安全 |
| `docs/superpowers/plans/2026-04-14-scene-aware-retrieval-redesign.md` | 历史实施计划 | 基本安全 |
| `docs/superpowers/plans/2026-04-27-xiaowang-system-quarterly-modification-plan.md` | 历史实施计划 | 基本安全 |
| `docs/superpowers/specs/2026-04-12-book-import-flow-design.md` | 历史设计稿 | 基本安全 |
| `docs/superpowers/specs/2026-04-12-book-management-design.md` | 历史设计稿 | 基本安全 |
| `docs/superpowers/specs/2026-04-13-claude-skill-routing-design.md` | 历史设计稿 | 基本安全 |
| `docs/superpowers/specs/2026-04-14-p0-fixes-design.md` | 历史设计稿 | 基本安全 |
| `docs/superpowers/specs/2026-04-14-scene-aware-retrieval-design.md` | 历史设计稿 | 基本安全 |
| `docs/superpowers/specs/2026-05-22-fix-entity-extraction-and-token-tracking-design.md` | 历史设计稿 | 基本安全 |
| `docs/superpowers/specs/2026-05-22-knowledge-graph-filtering-design.md` | 历史设计稿 | 基本安全 |
| `docs/validator_optimization_plan.md` | 旧优化计划 | 基本安全 |

## 1.2 删除依据是否充分

依据充分。上述文件都属于历史文档、旧计划、旧 specs 或基线说明，不在当前 FastAPI Dashboard 主路线内。

它们没有被以下当前主链路直接依赖：

- 无 `package.json`，不存在前端 npm 构建脚本引用。
- `pyproject.toml` 的 pytest 配置排除了 `docs/`。
- `novel_system/api.py`、`novel_system/service.py`、`static/app.js`、`templates/dashboard.html` 不依赖这些文档。
- 当前启动命令 `conda run -n chaishu python -m novel_system.api` 不依赖这些文档。

## 1.3 是否存在误删风险

运行误删风险：低。

文档追溯风险：中低。被删文件可能还能解释历史决策，但当前项目瘦身目标下，历史设计稿不是保留理由。真正需要保留的接管信息已经进入 `docs/00` 至 `docs/06`，本文件继续沉淀第二轮结论。

## 1.4 是否存在构建 / 测试 / 运行风险

当前没有明确风险。

- 项目无 `package.json`，没有前端构建链路。
- `pyproject.toml` 测试入口是 `tests/`，并排除 `docs/`。
- 轻量回归已通过。
- 第一轮删除没有触碰 `novel_system/`、`static/`、`templates/`、`tests/`、`requirements.txt`、`pyproject.toml`。

## 1.5 第一轮后仍明显遗留的冗余

仍然明显冗余，且可以继续激进清理：

- 根目录运行日志：`server.log`
- 忽略目录运行产物：`data/runtime/*.log`、`data/runtime/*.pid`、`data/runtime/predictions.jsonl`
- Python 缓存：`.pytest_cache/`、`__pycache__/`
- 根目录重复小说文本：`凡人修仙传(1-500章).txt`
- 根目录散落小说文本：`《诛仙》_qinkan.net.txt`
- 一次性 debug 脚本：`show_trace.py`
- 一次性迁移验证脚本：`scripts/verify_migration.py`
- 已生成评测报告产物：`data/eval_full/*`
- 不接入当前自动测试的手工回归资产：`p0_test_cases.jsonl`
- 旧 PRD：`tasks/*.md`
- 本地 agent / Claude 历史归档：`.agents/skills/ralph/archive/**`、`.claude/skills/ralph/archive/**`

---

# 2. 当前项目稳定性判断

## 2.1 是否可以继续清理

可以继续进入第二轮激进清理。

当前没有证据表明第一轮清理破坏主流程。第一轮删除集中在旧文档，主流程代码和配置没有被删除。

## 2.2 是否需要暂停清理

不需要暂停。

但以下对象不能直接删，只能进入“删除前人工确认”：

- `novel_system/retrieval.py`
- `start_server.py`
- `scripts/run_full_eval.py` + `eval_runner_template.py` + `fanren_eval_cases_v1.jsonl`
- `scripts/eval_retrieval.py` + `tests/test_retrieval_metrics.py` + `data/eval/fanren_retrieval_eval_v1.jsonl`
- `.worktrees/quarterly-phase1/`
- `.agents/skills/**`、`.claude/skills/**`
- `data/runtime/models/`
- `data/books/`
- `data/uploads/`

## 2.3 是否需要先运行构建 / 测试 / 页面检查

低风险删除前不需要暂停等待页面人工检查，但每批删除后必须验证。

当前项目没有 npm 构建。建议使用以下验证阶梯：

```bash
conda run -n chaishu python -m pytest tests/test_claude_skill_router.py
conda run -n chaishu python -m pytest tests/test_book_import_artifacts.py
conda run -n chaishu python -m pytest tests/test_retrieval_metrics.py
conda run -n chaishu python -m pytest
```

说明：

- 删除日志、缓存、旧文档后，跑 `test_claude_skill_router.py` 即可。
- 删除根目录重复小说文本后，必须跑 `test_book_import_artifacts.py`。
- 删除或移动评测脚本前后，必须跑 `test_retrieval_metrics.py` 或同步删除相关测试。
- 删除中风险脚本和数据后，建议跑完整 pytest。

## 2.4 是否需要人工启动项目检查页面功能

第二轮低风险删除后不强制，但建议在处理以下内容后人工启动检查页面：

- 删除根目录小说文本后。
- 删除或修改评测相关脚本和数据后。
- 删除 `.agents` / `.claude` 相关配置后。
- 任何触碰 `static/`、`templates/`、`novel_system/api.py` 的后续清理后。

人工页面检查命令：

```bash
conda run -n chaishu python -m novel_system.api
```

打开：

```text
http://localhost:8000
```

---

# 3. 第二批激进清理清单

## A. 建议直接删除

以下对象不属于当前主路线，或者是运行产物、旧文档、一次性脚本、可再生成报告。删除前只做引用或路径检查，不需要架构改造。

| 文件路径 | 文件类型 | 为什么判定为冗余 | 与当前主路线的关系 | 删除前是否需要检查 | 删除后需要验证什么 | 风险 |
|---|---|---|---|---|---|---|
| `server.log` | 日志 | 根目录运行日志，`.gitignore` 已忽略 `*.log` | 主路线运行不读取 | 不需要，除非正在排查问题 | `pytest tests/test_claude_skill_router.py` | 低 |
| `data/runtime/api.stdout.log` | 日志 | 运行时 stdout 产物 | 主路线可再生成 | 确认服务未在写入 | `pytest tests/test_claude_skill_router.py` | 低 |
| `data/runtime/api.stderr.log` | 日志 | 运行时 stderr 产物 | 主路线可再生成 | 确认服务未在写入 | `pytest tests/test_claude_skill_router.py` | 低 |
| `data/runtime/api_stdout.log` | 日志 | 旧命名 stdout 日志 | 主路线可再生成 | 确认服务未在写入 | `pytest tests/test_claude_skill_router.py` | 低 |
| `data/runtime/api_stderr.log` | 日志 | 旧命名 stderr 日志 | 主路线可再生成 | 确认服务未在写入 | `pytest tests/test_claude_skill_router.py` | 低 |
| `data/runtime/api_server.pid` | PID | 旧进程状态文件 | 不参与启动，启动会再生成 | `Get-Process -Id <pid>` 确认不是活进程 | `pytest tests/test_claude_skill_router.py` | 低 |
| `data/runtime/predictions.jsonl` | 运行产物 | 临时预测输出，且 `data/eval_full/predictions.jsonl` 已承担报告输出角色 | 页面主路线不读取 | 确认不需要这次临时评测结果 | `pytest tests/test_retrieval_metrics.py` | 低 |
| `.pytest_cache/` | 测试缓存目录 | pytest 缓存，可自动重建 | 与运行无关 | 不需要 | `pytest tests/test_claude_skill_router.py` | 低 |
| `凡人修仙传(1-500章).txt` | 根目录小说文本 | 与 `data/uploads/凡人修仙传(1-500章).txt` SHA256 一致，`.env` 指向 `data/uploads` | 当前默认导入不使用根目录副本 | 已确认 hash 一致，删除前再跑一次 `Get-FileHash` | `pytest tests/test_book_import_artifacts.py` | 低 |
| `show_trace.py` | 一次性 debug 脚本 | 硬编码 localhost 和固定问题，只生成 `trace_output.json`，不是通用工具 | 主路线不调用 | `rg -n "show_trace" .` 只应剩文档引用 | `pytest tests/test_claude_skill_router.py` | 低 |
| `scripts/verify_migration.py` | 一次性迁移脚本 | 只验证 US-006 图谱命名迁移，未被测试、入口或配置调用 | 主路线不调用 | `rg -n "verify_migration" .` 确认无代码调用 | `pytest tests/test_graph_name_policy.py` | 低 |
| `p0_test_cases.jsonl` | 手工回归数据 | 当前无代码引用，无 pytest 使用 | 当前测试流程不读取 | `rg -n "p0_test_cases" .` 确认无脚本接入 | `pytest tests/test_planner_intent.py` | 低 |
| `fanren_eval_readme.md` | 旧评测说明 | 只说明评测集，且文件名仍写 `fanren_eval_runner_template.py`，与实际不一致 | 主路线不读取 | 确认不作为交付文档 | `pytest tests/test_claude_skill_router.py` | 低 |
| `data/eval_full/predictions.jsonl` | 评测输出 | `scripts/run_full_eval.py` 可再生成 | 页面主路线不读取 | 确认不保留历史基线报告 | `pytest tests/test_claude_skill_router.py` | 低 |
| `data/eval_full/report.html` | 评测输出 | 可再生成 HTML 报告 | 页面主路线不读取 | 确认不保留历史基线报告 | `pytest tests/test_claude_skill_router.py` | 低 |
| `data/eval_full/report.json` | 评测输出 | 可再生成 JSON 报告 | 页面主路线不读取 | 确认不保留历史基线报告 | `pytest tests/test_claude_skill_router.py` | 低 |
| `data/eval_full/report_summary.txt` | 评测输出 | 可再生成摘要 | 页面主路线不读取 | 确认不保留历史基线报告 | `pytest tests/test_claude_skill_router.py` | 低 |
| `tasks/prd-graph-migration-name-policy.md` | 历史 PRD | 历史方案，不参与运行、测试、配置 | 主路线不读取 | 不需要 | `pytest tests/test_claude_skill_router.py` | 低 |
| `tasks/prd-project-restructure.md` | 历史 PRD | 历史重构方案，不参与运行 | 主路线不读取 | 不需要 | `pytest tests/test_claude_skill_router.py` | 低 |
| `tasks/prd-system-refactor.md` | 历史 PRD | 历史系统重构方案，不参与运行 | 主路线不读取 | 不需要 | `pytest tests/test_claude_skill_router.py` | 低 |
| `tasks/prd-vector-search-integration-fixes.md` | 历史 PRD | 历史修复方案，不参与运行 | 主路线不读取 | 不需要 | `pytest tests/test_claude_skill_router.py` | 低 |
| `.claude/scheduled_tasks.lock` | 工具锁文件 | 本地工具锁，不是项目运行配置 | 主路线不读取 | 确认无 Claude 定时任务正在使用 | `pytest tests/test_claude_skill_router.py` | 低 |
| `.agents/skills/ralph/archive/2026-04-30-graph-name-policy-migration/` | 历史 agent 归档 | 旧任务归档，非当前主路线 | 主路线不读取 | 确认不需要回看旧 agent 状态 | `pytest tests/test_claude_skill_router.py` | 低 |
| `.agents/skills/ralph/archive/2026-04-30-retrieval-upgrade-reranker/` | 历史 agent 归档 | 旧任务归档，非当前主路线 | 主路线不读取 | 同上 | `pytest tests/test_claude_skill_router.py` | 低 |
| `.agents/skills/ralph/archive/2026-05-03-vector-search-review-fixes/` | 历史 agent 归档 | 旧任务归档，非当前主路线 | 主路线不读取 | 同上 | `pytest tests/test_claude_skill_router.py` | 低 |
| `.agents/skills/ralph/archive/2026-05-04-graph-name-policy-migration/` | 历史 agent 归档 | 旧任务归档，非当前主路线 | 主路线不读取 | 同上 | `pytest tests/test_claude_skill_router.py` | 低 |
| `.agents/skills/ralph/archive/2026-05-04-vector-search-integration/` | 历史 agent 归档 | 旧任务归档，非当前主路线 | 主路线不读取 | 同上 | `pytest tests/test_claude_skill_router.py` | 低 |
| `.agents/skills/ralph/archive/2026-05-22-fix-entity-extraction-token-tracking/` | 历史 agent 归档 | 当前未跟踪归档，非主路线 | 主路线不读取 | 确认不需要回看旧 agent 状态 | `pytest tests/test_claude_skill_router.py` | 低 |
| `.agents/skills/ralph/archive/2026-05-22-graph-name-policy-migration/` | 历史 agent 归档 | 当前未跟踪归档，非主路线 | 主路线不读取 | 同上 | `pytest tests/test_claude_skill_router.py` | 低 |
| `.claude/skills/ralph/archive/2026-04-30-graph-name-policy-migration/` | 历史 agent 归档 | 旧任务归档，非当前主路线 | 主路线不读取 | 确认不需要回看旧 agent 状态 | `pytest tests/test_claude_skill_router.py` | 低 |
| `.claude/skills/ralph/archive/2026-04-30-retrieval-upgrade-reranker/` | 历史 agent 归档 | 旧任务归档，非当前主路线 | 主路线不读取 | 同上 | `pytest tests/test_claude_skill_router.py` | 低 |
| `.claude/skills/ralph/archive/2026-05-03-vector-search-review-fixes/` | 历史 agent 归档 | 旧任务归档，非当前主路线 | 主路线不读取 | 同上 | `pytest tests/test_claude_skill_router.py` | 低 |
| `.claude/skills/ralph/archive/2026-05-04-graph-name-policy-migration/` | 历史 agent 归档 | 旧任务归档，非当前主路线 | 主路线不读取 | 同上 | `pytest tests/test_claude_skill_router.py` | 低 |
| `.claude/skills/ralph/archive/2026-05-04-vector-search-integration/` | 历史 agent 归档 | 旧任务归档，非当前主路线 | 主路线不读取 | 同上 | `pytest tests/test_claude_skill_router.py` | 低 |
| `.claude/skills/ralph/archive/2026-05-22-graph-name-policy-migration/` | 历史 agent 归档 | 旧任务归档，非当前主路线 | 主路线不读取 | 同上 | `pytest tests/test_claude_skill_router.py` | 低 |

## B. 建议删除，但需人工确认

以下对象大概率冗余，但可能涉及手动流程、外部评测、外部启动习惯、git worktree 或 agent 工作流。确认后应删除，不建议长期保留。

| 文件路径 | 为什么怀疑冗余 | 不确定点是什么 | 人工需要确认什么 | 确认后如何删除 | 删除后验证什么 | 风险 |
|---|---|---|---|---|---|---|
| `《诛仙》_qinkan.net.txt` | 根目录散落大文本，无代码引用 | 当前未发现 `data/uploads` 同名备份，且文件未跟踪 | 是否仍用于多书导入、图谱测试或数据留存 | 若无需留存，直接删除；若需留存，先移入 `data/uploads/` 后删除根目录副本 | `pytest tests/test_book_import_artifacts.py` | 中 |
| `scripts/run_full_eval.py` | 离线完整评测脚本，不是服务启动、页面或 pytest 必需 | 可能被人工验收或外部评测机使用 | 是否仍把完整评测作为交付验收 | 若不用，连同 `eval_runner_template.py`、`fanren_eval_cases_v1.jsonl`、`data/eval_full/*` 一起删除 | `pytest` 完整测试 | 中 |
| `eval_runner_template.py` | 根目录评测模板，不属于主路线 | 被 `scripts/run_full_eval.py` import | 是否删除完整评测链路 | 与 `scripts/run_full_eval.py` 同批删除 | `pytest` 完整测试 | 中 |
| `fanren_eval_cases_v1.jsonl` | 评测案例数据，不是主路线输入 | 被 `scripts/run_full_eval.py` 使用 | 是否保留离线完整评测 | 与完整评测脚本同批删除 | `pytest` 完整测试 | 中 |
| `scripts/eval_retrieval.py` | 离线检索评测脚本，不是服务启动必需 | `tests/test_retrieval_metrics.py` 直接 import 其中指标函数 | 是否接受删除该评测脚本以及对应测试 | 同步删除或改写 `tests/test_retrieval_metrics.py`，并删除 `data/eval/fanren_retrieval_eval_v1.jsonl` | `pytest` 完整测试 | 中 |
| `tests/test_retrieval_metrics.py` | 只测试离线评测指标，不覆盖页面主路线 | 如果保留 `scripts/eval_retrieval.py` 就应保留此测试 | 是否保留检索评测工具链 | 若删除评测脚本，则同步删除此测试 | `pytest` 完整测试 | 中 |
| `data/eval/fanren_retrieval_eval_v1.jsonl` | 检索评测数据，不是运行输入 | 被 `scripts/eval_retrieval.py` 使用 | 是否保留检索评测工具链 | 与 `scripts/eval_retrieval.py` 同批删除 | `pytest` 完整测试 | 中 |
| `docs/project_takeover_analysis_framework.md` | 接管分析框架，不是运行文档 | 当前 00 至 08 文档已覆盖大部分接管信息，但用户可能仍把它当方法论来源 | 是否仍作为项目清理方法论保留 | 确认后删除；必要信息已在 00 至 08 固化 | `rg -n "project_takeover_analysis_framework" docs README AGENTS.md` | 低 |
| `start_server.py` | 与 `python -m novel_system.api` 功能重叠 | 文档和使用习惯仍把它作为快捷启动入口 | 是否统一只保留 `python -m novel_system.api` | 先改 `docs/02`、`README.md`、`AGENTS.md` 启动命令，再删除 | 人工启动页面 | 中 |
| `.worktrees/quarterly-phase1/` | 不属于运行主路线，占用项目目录 | 是已注册 git worktree，不是普通缓存目录 | 分支是否还有未提交或保留价值 | 使用 `git worktree remove .worktrees/quarterly-phase1`，不要手删目录 | `git worktree list` | 高 |
| `.agents/skills/**` | 项目内复制了一套 skills，AGENTS 又说优先复用全局 skills | 当前会话实际可见技能路径仍包含 `.agents/skills` | 是否已经迁到全局 skills，是否允许项目不再携带 skill 源文件 | 先确认全局可用，再删除项目内 skills | `pytest tests/test_claude_skill_router.py`，并重新打开 Codex/Claude 验证 | 高 |
| `.claude/skills/**` | 与 `.agents/skills` 高度重复，不属于 app 主路线 | Claude 本地可能仍读取这些 skills | 是否仍需要项目级 Claude skills | 先保留 `.claude/hooks/` 和 settings，删除 skills 目录 | `pytest tests/test_claude_skill_router.py`，人工验证 Claude 路由 | 高 |
| `.claude/settings.local.json` | 本地工具配置，不是项目运行配置 | 可能保存用户本地权限偏好 | 是否应纳入仓库 | 确认后删除或加入忽略 | `pytest tests/test_claude_skill_router.py` | 中 |
| `.agents/skills/ralph/prd.json` | 当前 agent 工作状态，不是 app 主路线 | 文件当前已修改，可能是用户正在进行的工作 | 是否仍有活跃 Ralph 任务 | 若无活跃任务，删除或归档整个 Ralph 工作态 | 不影响 app，验证 agent 工作流 | 中 |
| `.agents/skills/ralph/progress.txt` | 当前 agent 工作状态 | 文件当前已修改 | 同上 | 同上 | 同上 | 中 |
| `.agents/skills/ralph/.last-branch` | 当前 agent 工作状态 | 文件当前已修改 | 同上 | 同上 | 同上 | 中 |
| `.agents/skills/ralph/ralph.sh` | agent 辅助脚本 | 文件当前已修改 | 是否仍使用 Ralph | 若不使用 Ralph，删除 Ralph skill 或迁到全局 | agent 工作流验证 | 中 |
| `.claude/skills/ralph/prd.json` | Claude 侧 agent 工作状态 | 文件当前已修改 | 是否仍有活跃 Claude/Ralph 任务 | 确认后删除或归档 | agent 工作流验证 | 中 |

## C. 暂时保留，但后续重点观察

以下对象当前仍在主路线或测试路线中，第二轮不删。它们可以作为后续重构瘦身目标，但不是“只删除不改架构”阶段的对象。

| 文件路径 | 为什么暂时不删 | 为什么仍然可疑 | 后续如何判断是否能删 |
|---|---|---|---|
| `novel_system/retrieval.py` | `services/qa.py` 直接导入 `HybridRetriever, RetrievalHit`；`services/continuation.py` 也导入 `RetrievalHit` | 只是兼容 facade，真实实现已在 `search/orchestrator.py` | 先把 `qa.py`、`continuation.py`、相关测试迁到 `SearchOrchestrator` 和统一 hit 类型，再删 |
| `novel_system/search/orchestrator.py` | 当前真实检索引擎，测试大量覆盖 | 逻辑可能过宽，但属于主路线 | 只有在替代检索引擎完成且 API/QA 测试通过后才能动 |
| `novel_system/indexing.py` | 导入、切分、产物构建和索引仓库核心 | 文件偏大，职责偏宽 | 后续可拆分，但不是本轮删除对象 |
| `novel_system/services/indexing.py` | API 书籍导入和索引流程依赖 | 与 `BookIndexRepository` 私有方法耦合 | 后续重构索引边界时再评估 |
| `novel_system/service.py` | API 通过 `create_service()` 使用 | `NovelSystemService` 与 `GraphService` 边界不清 | 后续统一服务工厂前不能删 |
| `novel_system/graph_service.py` | `create_service()` 当前返回 `GraphService()` | 继承覆盖关系复杂 | 后续合并 graph/service 边界后再评估 |
| `templates/dashboard.html` | 当前唯一页面入口 | 无框架、体积集中 | 后续前端重构时再拆，不在删除轮处理 |
| `static/app.js` | 页面所有交互/API/图谱逻辑 | 文件大、职责集中 | 后续前端模块化前不能删 |
| `static/styles.css` | 当前页面样式依赖 | 可能有未用 CSS | 需页面级截图和 CSS 覆盖审计后再删 |
| `data/uploads/凡人修仙传(1-500章).txt` | `.env DEFAULT_BOOK_PATH` 当前指向它 | 大文件，但属于当前默认输入资产 | 若切换到外部数据源或明确不再需要默认书籍，可删 |
| `data/books/` | 当前索引产物，页面和问答读取 | 可再生成但成本高 | 只有确认愿意重建索引时再清 |
| `data/runtime/models/` | 本地 embedding 模型缓存 | 占空间但避免重新下载或转换 | 确认网络和模型下载路径可用后再清 |
| `.claude/hooks/global-skill-router.ps1` | `tests/test_claude_skill_router.py` 直接执行 | 不是 app 主路线，但当前开发路由测试依赖 | 若删除 Claude 路由测试或迁到 Codex 全局 hook，再删 |
| `.codex/hooks/global-skill-router.ps1` | Codex 路由配置 | 不是 app 主路线，但影响当前协作流程 | 确认不用项目级 Codex hook 后再删 |
| `tests/` | 当前质量基线 | 有些测试覆盖的是历史工具或旧兼容层 | 先删对应旧功能，再同步删测试 |

---

# 4. 脚本清理专项

## 必须保留

| 文件路径 | 是否被 package.json 引用 | 是否属于当前主路线 | 是否建议删除 | 删除前检查项 | 删除后验证项 |
|---|---|---|---|---|---|
| `start_server.py` | 无 `package.json` | 是，本地快捷启动入口 | 否，本轮保留 | 若要删，先统一启动命令为 `python -m novel_system.api` | 人工启动页面 |
| `.claude/hooks/global-skill-router.ps1` | 无 `package.json` | 非 app 主路线，但当前路由测试依赖 | 否，本轮保留 | `tests/test_claude_skill_router.py` 直接引用 | `pytest tests/test_claude_skill_router.py` |
| `.codex/hooks/global-skill-router.ps1` | 无 `package.json` | 非 app 主路线，但属于当前 Codex 工作流配置 | 否，本轮保留 | 确认是否还有 Codex hook 使用 | 人工验证 Codex 路由 |

## 建议删除

| 文件路径 | 是否被 package.json 引用 | 是否属于当前主路线 | 是否建议删除 | 删除前检查项 | 删除后验证项 |
|---|---|---|---|---|---|
| `show_trace.py` | 无 `package.json` | 否 | 是 | `rg -n "show_trace" .` | `pytest tests/test_claude_skill_router.py` |
| `scripts/verify_migration.py` | 无 `package.json` | 否 | 是 | `rg -n "verify_migration" .` | `pytest tests/test_graph_name_policy.py` |
| `.agents/skills/ralph/archive/**` | 无 `package.json` | 否 | 是 | 确认不需要历史 Ralph 归档 | `pytest tests/test_claude_skill_router.py` |
| `.claude/skills/ralph/archive/**` | 无 `package.json` | 否 | 是 | 确认不需要历史 Claude/Ralph 归档 | `pytest tests/test_claude_skill_router.py` |

## 待人工确认

| 文件路径 | 是否被 package.json 引用 | 是否属于当前主路线 | 是否建议删除 | 删除前检查项 | 删除后验证项 |
|---|---|---|---|---|---|
| `scripts/run_full_eval.py` | 无 `package.json` | 否，离线评测 | 确认后删除 | 是否外部评测机或人工验收仍调用 | `pytest` |
| `eval_runner_template.py` | 无 `package.json` | 否，但被 `scripts/run_full_eval.py` import | 跟随 full eval 链路删除 | 是否保留完整评测链路 | `pytest` |
| `scripts/eval_retrieval.py` | 无 `package.json` | 否，离线评测 | 确认后删除 | `tests/test_retrieval_metrics.py` 是否同步删除或改写 | `pytest` |
| `.agents/skills/ralph/ralph.sh` | 无 `package.json` | 否，agent 工具 | 确认后删除 | 是否仍使用 Ralph | agent 工作流验证 |
| `.agents/skills/**/scripts/*` | 无 `package.json` | 否，skill 内部工具 | 确认后删除 | 是否迁到全局 skills | Codex/Claude 技能可用性验证 |

---

# 5. 不建议清理的文件

以下文件暂时不要动。它们不是因为“可能未来有用”而保留，而是因为当前启动、页面、API、核心业务、配置或测试正在依赖。

| 文件路径 | 为什么现在不能删 | 后续如何判断是否可删 |
|---|---|---|
| `AGENTS.md` | 指定 conda 环境、测试命令、技能路由规则 | 项目完全移除 agent 协作规范后再评估 |
| `README.md` | 当前系统说明和启动/评测背景 | 新文档体系完全替代 README 后再评估 |
| `pyproject.toml` | pytest 发现规则和测试配置 | 测试框架迁移后再评估 |
| `requirements.txt` | Python 依赖声明 | 迁移到其他依赖管理后再评估 |
| `.env.example` | 环境变量模板 | 配置体系迁移后再评估 |
| `.gitignore` | 忽略 data/runtime、uploads、日志等产物 | 仓库策略变更后再评估 |
| `novel_system/api.py` | FastAPI 路由入口，挂载页面和 API | 新 API 入口替代后再评估 |
| `novel_system/config.py` | `.env` 和路径配置解析 | 配置体系替代后再评估 |
| `novel_system/models.py` | API、planner、trace、graph 数据契约 | 模型拆分后再评估 |
| `novel_system/service.py` | 服务工厂和主服务门面 | 服务工厂迁移后再评估 |
| `novel_system/graph_service.py` | 当前实际服务实例 | graph API 替代后再评估 |
| `novel_system/services/base.py` | 初始化 repo、LLM、planner、validator、reranker | 服务依赖注入重构后再评估 |
| `novel_system/services/qa.py` | `/ask` 主流程 | 新 QA 服务替代后再评估 |
| `novel_system/services/continuation.py` | `/continue` 主流程 | 新续写服务替代后再评估 |
| `novel_system/services/indexing.py` | 书籍导入和索引 API 逻辑 | 索引流程替代后再评估 |
| `novel_system/services/stats.py` | `/api/storage-stats`、`/api/token-stats`、`/api/dashboard` 依赖 | 页面移除统计区后再评估 |
| `novel_system/indexing.py` | 索引构建和 artifact 仓库核心 | 完成拆分和测试迁移后再评估 |
| `novel_system/retrieval.py` | 当前 QA/续写仍依赖其类型和兼容 retriever | 消除 `HybridRetriever` / `RetrievalHit` 引用后再删 |
| `novel_system/search/` | 当前检索编排主实现 | 替代检索引擎完成后再评估 |
| `novel_system/validator.py` | EvidenceGate、AnswerValidator、SpoilerGuard | 验证层替代后再评估 |
| `novel_system/llm.py` | LLM 客户端和 fallback | LLM 适配层替代后再评估 |
| `novel_system/embedding/` | 向量检索和索引构建依赖 | 放弃向量检索后再评估 |
| `novel_system/vector_store/` | FAISS 存储抽象和实现 | 放弃本地向量库后再评估 |
| `novel_system/reranker/` | 检索重排由 `services/base.py` 初始化 | 确认不再使用 rerank 后再删 |
| `novel_system/tracing.py` | QA/续写 debug trace 依赖 | debug trace 功能移除后再评估 |
| `templates/dashboard.html` | 当前唯一页面模板 | 新前端替代后再评估 |
| `static/app.js` | 当前页面交互主逻辑 | 新前端替代后再评估 |
| `static/styles.css` | 当前页面样式 | 页面替代后再评估 |
| `tests/test_claude_skill_router.py` | AGENTS 明确要求作为路由回归验证 | 删除项目级路由后再评估 |
| `data/uploads/凡人修仙传(1-500章).txt` | `.env DEFAULT_BOOK_PATH` 指向此文件 | 切换默认书籍来源后再评估 |
| `data/books/` | 当前索引产物，问答和页面读取 | 明确重建索引或释放空间时再清 |
| `data/runtime/models/` | 本地模型缓存，删除会导致重新下载或转换 | 确认网络、模型源、缓存路径可用后再清 |

---

# 6. 第二轮清理执行建议

## 第一小批：低风险直接删除

文件列表：

- `server.log`
- `data/runtime/api.stdout.log`
- `data/runtime/api.stderr.log`
- `data/runtime/api_stdout.log`
- `data/runtime/api_stderr.log`
- `data/runtime/api_server.pid`
- `data/runtime/predictions.jsonl`
- `.pytest_cache/`

删除理由：

- 全部是运行产物、缓存或状态文件。
- 不属于当前主路线源码。
- 删除后可自动再生成。

删除前检查：

```powershell
Get-Process -Id (Get-Content data/runtime/api_server.pid) -ErrorAction SilentlyContinue
```

删除后验证：

```bash
conda run -n chaishu python -m pytest tests/test_claude_skill_router.py
```

## 第二小批：根目录重复和一次性文件

文件列表：

- `凡人修仙传(1-500章).txt`
- `show_trace.py`
- `scripts/verify_migration.py`
- `p0_test_cases.jsonl`
- `fanren_eval_readme.md`

删除理由：

- `凡人修仙传(1-500章).txt` 已与 `data/uploads/凡人修仙传(1-500章).txt` hash 一致，且 `.env` 指向 `data/uploads`。
- `show_trace.py` 是硬编码本地 debug 请求脚本。
- `scripts/verify_migration.py` 是一次性迁移验证脚本。
- `p0_test_cases.jsonl` 当前无自动化入口。
- `fanren_eval_readme.md` 是旧评测说明，且内容中的模板文件名已过期。

删除前检查：

```powershell
Get-FileHash -Algorithm SHA256 '凡人修仙传(1-500章).txt'
Get-FileHash -Algorithm SHA256 'data/uploads/凡人修仙传(1-500章).txt'
Select-String -Path '.env' -Pattern 'DEFAULT_BOOK_PATH'
rg -n "show_trace|verify_migration|p0_test_cases|fanren_eval_readme" .
```

删除后验证：

```bash
conda run -n chaishu python -m pytest tests/test_book_import_artifacts.py
conda run -n chaishu python -m pytest tests/test_graph_name_policy.py
conda run -n chaishu python -m pytest tests/test_claude_skill_router.py
```

## 第三小批：评测报告产物

文件列表：

- `data/eval_full/predictions.jsonl`
- `data/eval_full/report.html`
- `data/eval_full/report.json`
- `data/eval_full/report_summary.txt`

删除理由：

- 都是运行 `scripts/run_full_eval.py` 后生成的报告产物。
- 当前页面主路线不读取这些文件。
- 若需要保留评测能力，保留脚本和输入集即可，不应把输出报告长期放在仓库主线。

删除后验证：

```bash
conda run -n chaishu python -m pytest tests/test_claude_skill_router.py
```

## 第四小批：旧 PRD 和 agent 归档

文件列表：

- `tasks/prd-graph-migration-name-policy.md`
- `tasks/prd-project-restructure.md`
- `tasks/prd-system-refactor.md`
- `tasks/prd-vector-search-integration-fixes.md`
- `.agents/skills/ralph/archive/**`
- `.claude/skills/ralph/archive/**`
- `.claude/scheduled_tasks.lock`

删除理由：

- 都是历史计划、历史任务状态或工具锁。
- 当前运行、页面、API、pytest 主流程不依赖。
- 当前阶段目标是收敛项目，而不是继续保留所有决策草稿。

删除后验证：

```bash
conda run -n chaishu python -m pytest tests/test_claude_skill_router.py
```

## 第五小批：需要人工确认后删除

文件列表：

- `《诛仙》_qinkan.net.txt`
- `scripts/run_full_eval.py`
- `eval_runner_template.py`
- `fanren_eval_cases_v1.jsonl`
- `scripts/eval_retrieval.py`
- `tests/test_retrieval_metrics.py`
- `data/eval/fanren_retrieval_eval_v1.jsonl`
- `docs/project_takeover_analysis_framework.md`
- `.worktrees/quarterly-phase1/`
- `.agents/skills/**`
- `.claude/skills/**`

人工确认项：

- 是否彻底放弃离线完整评测和检索评测工具链。
- 是否不再需要 `《诛仙》` 作为多书导入素材。
- 是否项目级 skills 已迁移到全局，删除 `.agents/skills` 和 `.claude/skills` 不影响协作。
- `.worktrees/quarterly-phase1/` 是否还有未提交内容或分支价值。
- `docs/project_takeover_analysis_framework.md` 的方法论信息是否已被 00 至 08 文档吸收。

确认后操作：

- 评测链路删除必须成组处理，避免留下孤儿测试或孤儿数据。
- `.worktrees/quarterly-phase1/` 必须用 `git worktree remove`，不要直接删目录。
- `.agents/skills/**`、`.claude/skills/**` 删除前先确认全局 skills 可用。

删除后验证：

```bash
conda run -n chaishu python -m pytest
git worktree list
```

---

# 7. 最终结论

可以继续清理，不需要暂停。

应该直接删：

- 运行日志、pid、pytest 缓存、runtime 临时输出。
- 根目录重复的 `凡人修仙传(1-500章).txt`。
- 一次性脚本 `show_trace.py`、`scripts/verify_migration.py`。
- 未接入自动流程的 `p0_test_cases.jsonl` 和过期评测说明 `fanren_eval_readme.md`。
- `data/eval_full/*` 评测输出报告。
- `tasks/*.md` 历史 PRD。
- Ralph / Claude 的历史 archive 和 `.claude/scheduled_tasks.lock`。

确认后删：

- `《诛仙》_qinkan.net.txt`
- 完整评测链路：`scripts/run_full_eval.py`、`eval_runner_template.py`、`fanren_eval_cases_v1.jsonl`
- 检索评测链路：`scripts/eval_retrieval.py`、`tests/test_retrieval_metrics.py`、`data/eval/fanren_retrieval_eval_v1.jsonl`
- 项目级 skills：`.agents/skills/**`、`.claude/skills/**`
- 注册 worktree：`.worktrees/quarterly-phase1/`

暂时别动：

- `novel_system/` 主链路代码。
- `templates/` 和 `static/`。
- `data/uploads/`、`data/books/`、`data/runtime/models/`。
- `pyproject.toml`、`requirements.txt`、`.env.example`、`.gitignore`、`AGENTS.md`、`README.md`。
- `novel_system/retrieval.py`，它虽然是 legacy facade，但仍在当前运行链路内。
