# 08 清理审核与第二批候选记录

> 生成日期：2026-05-25  
> 审核目标：完成 GraphRAG 架构迁移后的遗留文件清理验证。

## 1. 清理执行状态总结

截至 2026-05-25，第二批及后续激进清理已基本完成。

| 类别 | 清理项 | 状态 | 备注 |
|---|---|---|---|
| **运行产物** | `server.log`, `data/runtime/*.log`, `*.pid` | **已清理** | 目录已保持整洁。 |
| **重复素材** | 根目录 `凡人修仙传(1-500章).txt` | **已清理** | 统一使用 `data/uploads/` 路径。 |
| **离线评测** | `scripts/run_full_eval.py`, `eval_runner_template.py`, `fanren_eval_cases_v1.jsonl` | **已清理** | 项目转向 GraphRAG 实时问答验证。 |
| **遗留代码** | `novel_system/legacy/`, `tests/legacy/` | **已清理** | 彻底移除旧版 RAG 代码。 |
| **重构替代** | `novel_system/indexing.py` | **已处理** | 由 `novel_system/indexing/` 子包完全替代。 |
| **工具脚本** | `show_trace.py`, `scripts/verify_migration.py` | **已清理** | 历史调试工具已物理删除。 |

## 2. 剩余待观察项

| 路径 | 风险 | 处理建议 |
|---|---|---|
| `novel_system/retrieval.py` | **低** | 虽然主逻辑已空，但仍有类型定义。建议在确认无外部依赖后删除。 |
| `data/vectors/` | **低** | 旧版 FAISS 索引，不再被 GraphRAG 使用。 |
| `《诛仙》_qinkan.net.txt` | **低** | 散落在根目录，建议移入 `data/uploads/`。 |

## 3. 验证结论

清理后运行 `conda run -n chaishu python -m pytest`，全量测试用例通过。系统已成功“瘦身”，架构主线聚焦于 GraphRAG 适配层。
