# 04 文件用途审计表
> 生成日期：2026-05-25
> 分析范围：GraphRAG 迁移后项目主要文件扫描

## 核心组件文件表

| 文件路径 | 类型 | 主要作用 | 状态 | 处理建议 |
| :--- | :--- | :--- | :--- | :--- |
| `novel_system/graphrag_app/` | Package | **核心适配层**：管理 GraphRAG Workspace、查询引擎、索引运行器和数据投影。 | 活跃 | 核心模块，不可删除 |
| `novel_system/indexing/` | Package | **解析与仓库层**：负责书籍解析和 manifest 状态管理。 | 活跃 | 核心模块，不可删除 |
| `novel_system/services/` | Package | **业务逻辑层**：包含 qa.py (GraphRAG 搜索) 和 indexing.py (GraphRAG 索引流水线)。 | 活跃 | 核心模块，不可删除 |
| `novel_system/api.py` | Python | **Web 控制层**：暴露 FastAPI 端点，适配异步 GraphRAG 调用。 | 活跃 | 核心模块，不可删除 |
| `novel_system/models.py` | Python | **模型定义**：包含 GraphRAG 专用的请求/响应和 Trace 模型。 | 活跃 | 核心模块，不可删除 |
| `novel_system/retrieval.py` | Python | **Legacy 兼容层**：保留了旧版 hit 类型，主链路已迁移。 | 遗留 | 建议彻底清理 |
| `novel_system/validator.py` | Python | **验证层**：保留 EvidenceGate，SpoilerGuard 已移除。 | 活跃 | 需进一步重构 |
| `start_server.py` | Python | **启动入口**：使用 Uvicorn 启动服务。 | 活跃 | 保留 |

## 根目录文件审计

| 文件 | 状态 | 用途 | 处理建议 |
| :--- | :--- | :--- | :--- |
| `README.md` | 活跃 | 系统说明文档，已更新 GraphRAG 架构说明。 | 保留 |
| `AGENTS.md` | 活跃 | AI 协作规范，包含 Conda 环境和技能路由。 | 保留 |
| `requirements.txt` | 活跃 | 依赖声明，已新增 Pandas, PyArrow 等。 | 保留 |
| `.env.example` | 活跃 | 环境变量模板，新增 GraphRAG 配置项。 | 保留 |
| `《诛仙》_qinkan.net.txt` | 存在 | 原始小说测试素材。 | 建议移入 data/uploads/ |
| `eval_runner_template.py` | **已删除** | 旧评测模板。 | 已清理 |
| `show_trace.py` | **已删除** | 旧调试脚本。 | 已清理 |
| `fanren_eval_cases_v1.jsonl` | **已删除** | 旧评测用例。 | 已清理 |

## 数据目录审计 (`data/`)

| 路径 | 用途 | 处理建议 |
| :--- | :--- | :--- |
| `data/books/{book_id}/graphrag/` | **GraphRAG 主目录**：包含 input, output (parquet), settings.yaml, logs。 | **核心运行数据，不可删除** |
| `data/books/{book_id}/derived/` | **派生视图**：从 parquet 转换而来的 graph_view.json 和 timeline_view.json。 | 可再生成 |
| `data/uploads/` | 上传的小说源文件。 | 保留 |
| `data/vectors/` | 旧版 FAISS 向量索引。 | **已废弃，建议清理** |

## 审计结论
项目已完成从“混合检索”到“GraphRAG”的架构升级。根目录和 `novel_system/` 下的大量遗留兼容文件已清理或标记为 Legacy。当前系统运行高度依赖 `data/books/` 下的 GraphRAG Workspace 产物。
