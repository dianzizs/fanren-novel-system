# 05 疑似冗余文件清单
> 生成日期：2026-05-25
> 分析范围：GraphRAG 架构迁移及遗留代码清理后审计。

## 审计现状
由于 2026-05-24 至 05-25 期间进行了大规模的遗留代码清理（Physical Delete of Legacy Directories），原清单中的大部分文件已处理。

---

## 剩余疑似冗余文件

| 序号 | 文件路径 | 当前审计状态 | 风险等级 | 处理建议 |
| :--- | :--- | :--- | :--- | :--- |
| 1 | `novel_system/retrieval.py` | **疑似冗余** | 高 | 虽然保留了部分类型定义（如 `RetrievalHit`），但主逻辑已空。建议在确认前端/测试无残留引用后彻底删除。 |
| 2 | `data/vectors/` (目录) | **确认无引用** | 中 | 旧版 FAISS 向量索引数据。GraphRAG 已使用其内部向量管理机制。 | 建议在备份后物理删除。 |
| 3 | `novel_system/validator.py` 中的 `EvidenceGate` | 待重构 | 中 | 当前实现仍基于旧语义评分逻辑，建议迁移至 GraphRAG 语义一致性验证。 | 待重构。 |
| 4 | `novel_system/novel_heuristics.py` | 待确认 | 低 | 包含硬编码的小说领域规则（如绿液用途）。 | 确认是否将其逻辑整合入 GraphRAG 的自定义 Prompt 或社区报告。 |

## 已清理/已处理项记录

- `eval_runner_template.py`: **已物理删除**。
- `show_trace.py`: **已物理删除**。
- `fanren_eval_cases_v1.jsonl`: **已物理删除**。
- `novel_system/legacy/`: **已物理删除**。
- `tests/legacy/`: **已物理删除**。
- `凡人修仙传(1-500章).txt` (根目录): **已物理删除**。
