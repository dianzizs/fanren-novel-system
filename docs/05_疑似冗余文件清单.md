# 05 疑似冗余文件清单
> 生成日期：2026-05-23
> 分析范围：经过静态依赖扫描与文本搜索后，识别出的疑似多余、遗留、或散落位置不当的文件。

> [!WARNING]
> 本清单目前仅作为审计和评估产物。在本轮任务中，**严禁对物理文件进行任何删除、移动或修改**。请待人工确认后再进行操作。

## 审计定义区别说明
为避免误删有用文件，系统严格区分以下三种状态：
- **确认无引用**：静态代码分析、全文搜索和文本正则检索中均未发现其被任何代码/配置直接或间接加载。但可能是作为开发者的独立命令行工具使用。
- **疑似冗余**：虽然系统有引用（例如某些过时的测试用例还在依赖此兼容类），但其业务逻辑实际上已全部迁移至新版模块，继续保留会增加维护负债。
- **可以删除**：前两者经过人工开发确认，在修改完依赖后，可以安全从物理磁盘和 git 版本控制中移除的文件。**（本轮由于限制，暂不支持下达此直接删除结论）**

---

## 疑似冗余文件详细审计表

| 序号 | 文件路径 | 当前审计状态 | 风险等级 | 引用数量 | 关键引用方 | 无法判断原因 / 冗余细节 | 建议操作 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| 1 | [`novel_system/retrieval.py`](file:///d:/pythonProject/小王一号/小王一号/novel_system/retrieval.py) | 疑似冗余 | **高** | 2 | `tests/test_vector_retrieval.py`<br>`tests/test_semantic_scorer.py` | 旧版本的检索逻辑封装，目前已被新重构的 `novel_system/search/orchestrator.py` 全面替代。但目前仍有历史测试文件在引用它作为断言目标。 | 建议在后续开发中将相关 test 用例重构为引用 orchestrator，并将此文件在下一轮中彻底删除。 |
| 2 | [`eval_runner_template.py`](file:///d:/pythonProject/小王一号/小王一号/eval_runner_template.py) | 确认无引用 | **中** | 0 | 无 | 散落在根目录的评测模板。无外部代码引用。通常属于离线命令行脚本。 | 确认是否仍有自动化测试脚本或离线评测机以根目录路径执行此文件，若无，应将其移动归类到 `scripts/`。 |
| 3 | [`show_trace.py`](file:///d:/pythonProject/小王一号/小王一号/show_trace.py) | 确认无引用 | **中** | 0 | 无 | 终端独立查看 Trace 记录的辅助小工具，在其他模块内无导入关系。 | 如果开发团队认可，可以保留该脚本，但应将其移到 `scripts/` 中以避免根目录杂乱。 |
| 4 | [`凡人修仙传(1-500章).txt`](file:///d:/pythonProject/小王一号/小王一号/凡人修仙传(1-500章).txt) | 疑似冗余 | **高** | 0 | 无 | 小说原文本，在根目录属于违规散落放置。该数据本应进入 `data/` 目录；如果 data 目录已存在相同文件，根目录下这本原始文本则属于重复冗余。 | 人工确认后，将此文件移动到 `data/` 或在有备份的前提下标记为可删除。 |
| 5 | [`《诛仙》_qinkan.net.txt`](file:///d:/pythonProject/小王一号/小王一号/《诛仙》_qinkan.net.txt) | 疑似冗余 | **高** | 0 | 无 | 诛仙原文本，用于多本小说索引提取测试，在根目录下散落放置。同上，不应置于根目录。 | 人工确认是否已在 `data/` 中完成备份，并在下一轮清理归档。 |

---

## 下一轮人工确认的问题列表（行动指南）

请开发人员针对以下几点进行最终确认，以支持下一轮的冗余清理工作：

### 1. 兼容性检索重构（针对 `novel_system/retrieval.py`）
- **问**：我们可以重构 `tests/test_vector_retrieval.py` 和 `tests/test_semantic_scorer.py` 使其完全使用 `SearchOrchestrator` 代替吗？
- **答**：好

### 2. 根目录大型文本处理
- **问**：`凡人修仙传(1-500章).txt` 和 `《诛仙》_qinkan.net.txt` 是否已经在 `data/uploads/` 或其他非 Git 管理的本地路径中备份妥当？
- **答**：不需要

### 3. 工具脚本归档路径
- **问**：是否同意新建/调整如下目录结构？
  - 将 `start_server.py` 移动至 `scripts/start_server.py`（API 的快捷加载器）；
  - 将 `show_trace.py` 移动至 `scripts/show_trace.py`；
  - 将 `eval_runner_template.py` 移动至 `scripts/eval_runner_template.py`。
- **答**：同意
