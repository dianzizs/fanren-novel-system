# 10 待办与风险

> 生成日期：2026-05-23  
> 范围：当前清理后项目状态、文档同步风险、后续验证和结构优化。  
> 说明：所有不能直接从代码或文件树确认的事项均标注“待确认”。

## P0：需要优先确认的问题

| 优先级 | 问题 | 风险 | 建议动作 |
|---|---|---|---|
| P0 | 待确认：正式启动命令不一致 | `start_server.py` 可以启动 uvicorn；AGENTS 和部分文档提到 `python -m novel_system.api`，但当前 `api.py` 未见命令行启动逻辑 | 人工确认正式命令。若保留 `python -m novel_system.api`，需补启动逻辑或修正文档 |
| P0 | 待确认：评测链路是否正式废弃 | `scripts/run_full_eval.py`、`scripts/eval_retrieval.py`、评测数据和 `tests/test_retrieval_metrics.py` 已删除 | 决定是否恢复、替代或确认废弃；废弃后同步 README/docs |
| P0 | 待确认：清理后完整回归是否已跑 | `docs/08` 记录轻量测试通过，但当前工作区又有多处业务代码修改和文件删除 | 在合并或交付前运行 `conda run -n chaishu python -m pytest` |
| P0 | 待确认：页面主流程是否仍可用 | 文档同步未启动服务；清理涉及脚本、数据和前端/后端已有修改状态 | 人工启动 Dashboard，验证书籍列表、索引、阅读、问答、续写、图谱 |
| P0 | 待确认：`data/books/`、`data/uploads/`、`data/vectors/` 的仓库策略 | 这些目录含当前运行资产，删除会影响页面、问答或重建成本 | 不要直接删除；先确认是否可重建、是否需要备份 |

## P1：不能轻易删除的文件和目录

| 路径 | 原因 | 删除前必须确认 |
|---|---|---|
| `novel_system/` | 当前后端主链路 | 替代模块、测试迁移、完整 pytest |
| `templates/dashboard.html` | 当前唯一页面入口 | 新页面替代并人工验证 |
| `static/app.js`、`static/styles.css` | 当前前端交互和样式 | 新前端或模块化完成并截图/人工验证 |
| `tests/` | 当前质量基线 | 对应功能废弃或迁移后再删测试 |
| `start_server.py` | 当前实际 uvicorn 启动脚本 | 启动命令统一后再评估 |
| `pyproject.toml` | pytest 配置 | 测试框架迁移后再评估 |
| `requirements.txt` | 依赖声明 | 依赖管理工具迁移后再评估 |
| `.env.example` | 环境变量模板 | 配置体系替代后再评估 |
| `.gitignore` | 忽略运行产物和本地文件 | 仓库策略变更后再评估 |
| `AGENTS.md` | conda 环境、测试命令、协作路由 | 项目不再需要 agent 协作规范后再评估 |
| `README.md` | 用户入口说明 | 新文档体系完全替代后再评估 |
| `data/uploads/凡人修仙传(1-500章).txt` | 当前上传文本资产 | 待确认默认书籍是否仍指向它 |
| `data/books/` | 当前索引产物 | 确认可重建并接受重建成本 |
| `data/vectors/` | 当前 FAISS 向量索引 | 确认向量索引可重建 |
| `.agents/skills/**` | 当前仍有项目级 skill 工作态 | 待确认已迁移到全局且当前任务不依赖 |
| `.claude/skills/**` | Claude 可能仍读取项目级 skills | 待确认 Claude 工作流不依赖 |
| `.codex/hooks/` | Codex 项目级 hook/config | 待确认不再需要项目级 hook |

## P1：需要人工运行验证的功能

| 功能 | 建议命令或检查 | 成功标准 |
|---|---|---|
| 单元/集成测试 | `conda run -n chaishu python -m pytest` | 全部通过 |
| 启动服务 | `conda run -n chaishu python start_server.py` | 能访问 `http://localhost:8000` |
| 待确认启动命令 | `conda run -n chaishu python -m novel_system.api` | 待确认：若期望启动服务，应保持进程并监听端口 |
| 书籍列表 | 打开 Dashboard 或请求 `GET /api/books` | 返回当前书籍 manifest |
| 上传/注册书籍 | Dashboard 上传 `.txt` 或 `POST /api/books` | manifest 创建成功，状态合理 |
| 索引构建 | `POST /api/books/{book_id}/start-index` 或页面点击索引 | 状态从 pending/indexing 到 indexed |
| 阅读台 | `GET /api/books/{book_id}/reader` | 返回章节、当前章节、人物和 timeline |
| 证据问答 | `POST /api/books/{book_id}/ask`，可带 `debug: true` | 返回答案、证据、confidence；debug 时返回 trace |
| 续写 | `POST /api/books/{book_id}/continue`，可带 `debug: true` | 返回续写文本、验证信息；debug 时返回 trace |
| 图谱 | `GET /api/books/{book_id}/graph` | 返回 nodes/edges/timeline 等可渲染数据 |
| Dashboard 统计 | `GET /api/dashboard` | 待确认：`data/eval_full/` 清空后仍有合理响应 |
| Token 统计 | `GET /api/token-stats` | 返回已记录 token 用量 |

## P2：需要继续确认的问题

- 待确认：`docs/07_冗余文件清理记录.md` 是否需要补建，还是由 `docs/08` 和 `docs/09` 覆盖。
- 待确认：`docs/03_核心模块初步说明.md` 是否保留为兼容入口，还是后续删除。
- 待确认：`scripts/` 只剩 `__pycache__/` 后是否保留空目录。
- 待确认：`tasks/` 空目录是否保留。
- 待确认：`data/eval/`、`data/eval_full/` 空目录是否保留。
- 待确认：根目录 `《诛仙》_qinkan.net.txt` 是否仍用于多书导入、图谱验证或人工数据留存。
- 待确认：`.env` 中默认书籍路径是否仍指向 `data/uploads/凡人修仙传(1-500章).txt`，不要在文档中展开敏感值。
- 待确认：`README.md` 是否与 00 至 10 文档同步。

## P2：后续可以优化的结构问题

| 模块 | 问题 | 后续方向 |
|---|---|---|
| `novel_system/indexing.py` | 文件约 1700 行，包含解析、artifact、LLM 提取、向量、场景、人物注册表 | 按 artifact builder、repository、vector indexing、LLM extraction 拆分，待确认 |
| `novel_system/service.py` + `graph_service.py` | `create_service()` 返回 `GraphService()`，并覆盖 service 同名方法 | 明确服务门面和图谱服务边界，考虑组合替代继承，待确认 |
| `novel_system/retrieval.py` | legacy facade 仍被 QA/续写依赖 | 迁移到 `SearchOrchestrator` 和统一 hit 类型后再评估删除，待确认 |
| `novel_system/validator.py` | QA 验证、续写验证、剧透防护集中 | 按验证领域拆分，待确认 |
| `novel_system/models.py` | 所有 Pydantic 模型集中 | 按 API、trace、graph、eval、token 拆分，待确认 |
| `novel_system/llm.py` | `MiniMaxClient` 既像路由器又像具体客户端；service/repo 多处实例化 | 重命名路由器并统一 LLM 生命周期，待确认 |
| `planner.py` + `graph_name_policy.py` | 别名和名称策略分散 | 统一 alias/profile 数据源，待确认 |
| `static/app.js` | 前端交互全部集中 | 按 book、reader、qa、continuation、graph、dashboard 拆分，待确认 |
| `templates/dashboard.html` | 单模板承载所有页面结构 | 拆 partial 或引入更清晰的页面组织，待确认 |
| `docs/` | 03 新旧文件并存，07 缺失 | 确认文档编号策略后收敛 |

## P3：清理后建议的验证阶梯

| 清理类型 | 最低验证 | 更稳妥验证 |
|---|---|---|
| 仅文档更新 | `git diff -- docs` | 不需要运行业务测试 |
| 删除缓存、日志、空目录 | `conda run -n chaishu python -m pytest tests/test_claude_skill_router.py` | 完整 pytest |
| 删除数据文本或索引产物 | `conda run -n chaishu python -m pytest tests/test_book_import_artifacts.py` | 页面人工验证导入、索引、阅读、问答 |
| 删除评测链路 | 完整 `conda run -n chaishu python -m pytest` | 同步更新 README/docs，并人工确认不再需要离线评测 |
| 删除 agent/Claude/Codex 配置 | `conda run -n chaishu python -m pytest tests/test_claude_skill_router.py` | 重启相关工具后验证技能路由 |

## 当前结论

当前可以继续做文档同步和风险收敛，但不要在未确认前继续删除核心数据、主链路代码、测试基线或项目级协作配置。下一步最有价值的是统一启动命令、确认评测链路取舍，并跑一次完整 pytest。
