# 09 变更记录

> 生成日期：2026-05-23  
> 范围：记录当前项目接管扫描、文档重建、清理审核和本次文档同步动作。  
> 说明：以下记录基于当前工作区状态和已有文档；无法确认操作者或准确执行顺序的内容标注“待确认”。

### 2026-05-26 refactor 清理旧 JSON 产物读取入口与文档残留路由

- **涉及文件**：`novel_system/services/stats.py`, `novel_system/indexing/repository.py`, `docs/02_技术栈与启动流程.md`, `docs/09_变更记录.md`
- **变更内容**：
  1. 清理 `ARTIFACT_LABELS`，移除全部已废弃条目（`chapter_chunks`、`chapter_summaries`、`event_timeline`、`character_card`、`relationship_graph`、`world_rule`、`canon_memory`、`recent_plot`、`style_samples`、`vision_parse`）以及无生成代码的 `scene_segments`、`character_registry`，仅保留 `manifest` 和 `chapters`；
  2. 清理 `read_artifact()` 映射表，同步移除上述废弃条目，使 `/api/books/{id}/artifacts` 接口仅返回当前仍在生成的产物；
  3. 删除 `docs/02_技术栈与启动流程.md` 路由表中已不存在的 `POST /api/books/{book_id}/index` 条目，将活跃路由数从 22 更正为 21。
- **原因**：当前索引流程已全面迁移至 GraphRAG，旧 JSON 产物不再生成，但读取/展示入口仍存在，导致前端显示历史残留文件；文档中也遗留了已下线路由。
- **验证**：`python -c` 验证 `stats.py` 和 `repository.py` 模块导入正常。

### 2026-05-26 feat 增加关闭 Dashboard 窗口后自动释放端口设置

- **涉及文件**：`novel_system/config.py`, `novel_system/api.py`, `static/js/main.js`, `tests/test_mimo_config.py`, `tests/test_dashboard_book_actions.py`, `.env.example`, `docs/02_技术栈与启动流程.md`, `docs/09_变更记录.md`
- **变更内容**：
  1. 新增 `SHUTDOWN_ON_WINDOW_CLOSE` 和 `SHUTDOWN_GRACE_PERIOD_SEC` 配置，默认关闭；
  2. 新增 Dashboard 窗口心跳和关闭信号 API，开启配置后在关闭窗口并超过宽限期时结束后端进程；
  3. 前端启动后发送窗口心跳，并在 `pagehide` 时通过 `sendBeacon` 发送关闭信号；
  4. 同步更新环境变量示例、路由文档和回归测试。
- **原因**：本地开发时关闭浏览器窗口后希望自动结束服务进程并释放 3000 端口，同时避免刷新页面误杀服务。
- **验证**：先运行 `conda run -n chaishu python -m pytest tests/test_mimo_config.py tests/test_dashboard_book_actions.py -v` 观察到新增测试失败；实现后同命令 11 passed。随后运行 `conda run -n chaishu python -m pytest`，188 passed。

### 2026-05-26 fix 修改服务端口为 3000

- **涉及文件**：`start_server.py`, `novel_system/config.py`, `docs/00_项目总览.md`, `docs/02_技术栈与启动流程.md`, `README.md`, `docs/09_变更记录.md`
- **变更内容**：
  1. 修改 `start_server.py`，将 Uvicorn 服务监听端口从 8000 变更为 3000；
  2. 修改 `novel_system/config.py`，将 GraphRAG 本地 Embedding 服务的默认接口地址 `graphrag_embedding_api_base` 从 `http://localhost:8000/v1` 变更为 `http://localhost:3000/v1`；
  3. 同步修改 `docs/00_项目总览.md`、`docs/02_技术栈与启动流程.md` 以及 `README.md` 中对应的端口与测试接口说明。
- **原因**：根据用户要求将服务端口修改为 3000，且为了防止 GraphRAG 索引流程找不到本地 Embedding Server，同步更新了 embedding API 基础路由的默认端口配置。
- **验证**：运行 `conda run -n chaishu python -m pytest`，全量 184 个测试全部 PASS。

### 2026-05-25 feat 显示 GraphRAG 索引阶段细节

- **涉及文件**：`static/js/main.js`, `static/css/components.css`, `tests/test_dashboard_book_actions.py`, `docs/09_变更记录.md`
- **变更内容**：在书籍详情页进度条下方显示状态接口返回的实时阶段消息，例如“正在汇总实体关系描述... (40/987)”，并补充小字样式和回归测试。
- **原因**：GraphRAG CLI 的实体关系汇总阶段耗时较长，单纯百分比不足以判断任务是否继续推进。
- **验证**：先运行 `conda run -n chaishu python -m pytest tests/test_dashboard_book_actions.py -v` 观察到新增测试失败；实现后同命令 3 passed。随后运行 `conda run -n chaishu python -m pytest`，184 passed。

## 当前文档动作

| 时间 | 动作 | 产物 | 说明 |
|---|---|---|---|
| 2026-05-23 | 项目总览文档重建 | `docs/00_项目总览.md` | 梳理项目定位、主链路、技术栈和已知风险 |
| 2026-05-23 | 目录结构文档创建 | `docs/01_目录结构说明.md` | 旧版包含已不存在文件，本次已同步为当前真实目录状态 |
| 2026-05-23 | 技术栈和启动流程文档创建 | `docs/02_技术栈与启动流程.md` | 启动命令与真实 `start_server.py` 是否完全一致待确认 |
| 2026-05-23 | 核心模块初步说明创建 | `docs/03_核心模块初步说明.md` | 本次已替换为过渡入口，稳定版迁移到 `docs/03_核心模块说明.md` |
| 2026-05-23 | 文件用途审计表创建 | `docs/04_文件用途审计表.md` | 作为清理判断依据之一 |
| 2026-05-23 | 疑似冗余文件清单创建 | `docs/05_疑似冗余文件清单.md` | 作为第一轮和第二轮清理参考 |
| 2026-05-23 | 项目接管审查报告创建 | `docs/06_项目接管审查报告.md` | 汇总接管审查结论 |
| 2026-05-23 | 清理审核与第二批候选创建 | `docs/08_清理审核与第二批候选.md` | 记录第一轮删除评估、第二批候选和验证建议 |
| 2026-05-23 | 变更记录新增 | `docs/09_变更记录.md` | 本文件 |
| 2026-05-25 | 文档体系全面同步 | `docs/00` 至 `docs/10` | 更新架构描述为 Microsoft GraphRAG v3，同步已清理的遗留文件状态，删除过渡文档 |

## 2026-05-25 docs 全面同步 GraphRAG 架构

- **涉及文件**：`docs/00` 至 `docs/10` 全系列文档（不含 07，已合并入 08）。
- **变更内容**：
  1. **同步架构**：将项目定位、核心能力、模块说明从“混合检索”全面修正为“基于 GraphRAG 的全书分析”。
  2. **同步目录**：新增 `novel_system/graphrag_app/` 描述，确认 `indexing/` 已重构。
  3. **清理状态同步**：将已物理删除的 `eval_runner_template.py`、`legacy/` 目录等标记为“已清理”，更正文件用途表。
  4. **删除过渡文档**：物理删除了 `docs/03_核心模块初步说明.md`。
- **原因**：项目已完成 GraphRAG 核心迁移，旧文档（5月23日版）存在严重误导性，需确保“文实相符”。
- **验证**：人工核对代码实现（`qa.py`, `NovelSystemBase`）与文档描述一致。

## 已发生的清理状态

以下状态来自当前工作区 `git status --short`、`rg --files` 和目录扫描。待确认：这些删除动作发生在本次文档同步之前，具体执行批次和操作者需以历史记录或人工说明为准。

### 文档和历史计划清理

| 路径 | 当前状态 | 说明 |
|---|---|---|
| `docs/baseline/config.md` | 已删除 | 旧基线文档 |
| `docs/baseline/modules.md` | 已删除 | 旧基线文档 |
| `docs/superpowers/plans/*.md` | 已删除多项 | 历史实施计划 |
| `docs/superpowers/specs/*.md` | 已删除多项 | 历史设计稿 |
| `docs/validator_optimization_plan.md` | 已删除 | 旧优化计划 |
| `docs/07_冗余文件清理记录.md` | 当前不存在 | 待确认：是否从未创建、已删除，或文件名不一致 |

### 评测链路和脚本清理

| 路径 | 当前状态 | 说明 |
|---|---|---|
| `scripts/eval_retrieval.py` | 已删除 | 检索评测脚本 |
| `scripts/run_full_eval.py` | 已删除 | 完整评测脚本 |
| `scripts/verify_migration.py` | 已删除 | 一次性迁移验证脚本 |
| `eval_runner_template.py` | 已删除 | 完整评测模板 |
| `fanren_eval_cases_v1.jsonl` | 已删除 | 完整评测用例 |
| `fanren_eval_readme.md` | 已删除 | 旧评测说明 |
| `p0_test_cases.jsonl` | 已删除 | 手工回归数据 |
| `tests/test_retrieval_metrics.py` | 已删除 | 检索评测指标测试 |
| `data/eval/fanren_retrieval_eval_v1.jsonl` | 已删除 | 检索评测数据 |
| `data/eval_full/*` | 已删除 | 完整评测报告产物 |

### 根目录和历史 PRD 清理

| 路径 | 当前状态 | 说明 |
|---|---|---|
| `show_trace.py` | 已删除 | 一次性 trace debug 脚本 |
| `test_api.py` | 当前不存在 | 待确认：是否早于本轮已清理 |
| `test_read_log.py` | 当前不存在 | 待确认：是否早于本轮已清理 |
| `凡人修仙传(1-500章).txt` | 已删除 | 根目录重复小说文本；`data/uploads/` 内仍存在 |
| `tasks/*.md` | 已删除 | `tasks/` 当前为空 |
| `.worktrees/quarterly-phase1/` | 当前不存在 | `.worktrees/` 当前为空，`git worktree list` 只显示主工作区 |

### Agent/Claude 归档清理

| 路径 | 当前状态 | 说明 |
|---|---|---|
| `.agents/skills/ralph/archive/**` | 多项已删除 | 历史 Ralph 任务归档 |
| `.claude/skills/ralph/archive/**` | 多项已删除 | 历史 Claude/Ralph 任务归档 |
| `.claude/scheduled_tasks.lock` | 已删除 | 本地工具锁文件 |

## 仍保留的关键对象

| 路径 | 保留原因 |
|---|---|
| `novel_system/` | 当前后端主链路 |
| `templates/dashboard.html` | 当前唯一页面模板 |
| `static/app.js`、`static/styles.css` | 当前前端交互和样式 |
| `tests/` | 当前质量基线，仍有 34 个 `.py` 测试文件 |
| `data/books/` | 当前索引产物 |
| `data/uploads/凡人修仙传(1-500章).txt` | 当前上传文本资产，是否为默认书籍输入待确认 |
| `data/vectors/` | 当前向量索引 |
| `.agents/`、`.claude/`、`.codex/` | 当前协作工具配置，是否迁移到全局待确认 |
| `start_server.py` | 当前实际 uvicorn 启动脚本 |
| `README.md`、`AGENTS.md`、`pyproject.toml`、`requirements.txt`、`.env.example`、`.gitignore` | 项目说明、协作、测试、依赖和配置基础文件 |

## 本次文档同步动作

| 动作 | 文件 | 说明 |
|---|---|---|
| 更新 | `docs/01_目录结构说明.md` | 删除旧文档中已经不存在的根目录文件描述，补充 `.codex/`、空目录、已清理目录和待确认项 |
| 新增 | `docs/03_核心模块说明.md` | 将“初步说明”稳定化，补充入口、依赖、被调用方、调用关系和边界问题 |
| 更新 | `docs/03_核心模块初步说明.md` | 改为过渡入口，避免旧内容继续误导 |
| 新增 | `docs/09_变更记录.md` | 记录目前文档和清理动作 |
| 新增 | `docs/10_待办与风险.md` | 按优先级列出待确认、不可轻删、人工验证和结构优化 |

## 2026-05-23 AI 开发规则体系建立

### docs — 新建 AI 开发规范文档

| 动作 | 文件 | 说明 |
|------|------|------|
| 覆盖更新 | `AGENTS.md` | 重写为完整行为契约（v2.0），修正启动命令错误，新增修改声明格式、禁止行为清单、文档更新触发表、变更记录格式、测试检查清单 |
| 新增 | `docs/11_AI开发工作流规范.md` | 七类任务的标准流程：新需求、Bug修复、文件清理、页面改造、组件新增、接口变更、文档更新 |
| 新增 | `docs/12_上下文恢复指南.md` | 上下文恢复顺序、架构调用链全图、模块定位表、设计决策存储位置、防遗忘操作规范 |

**原因**：项目从 vibe coding 状态转向文档驱动 + 小步修改开发方式，需要明确约束 AI 编码行为。

**验证**：文档类变更，无需运行测试。

---

### 2026-05-24 fix/feat 移除剧透判定并修复语法错误

- **涉及文件**：`novel_system/services/qa.py`, `novel_system/services/continuation.py`, `docs/03_核心模块说明.md`
- **变更内容**：
  1. 修复了 `qa.py` 文件末尾残留的语法错误（删除了行末由于合并产生的冗余字符）。
  2. 移除了 Q&A 问答服务 `ask()` 和续写服务 `continue_story()` 中对于 `SpoilerGuard` 剧透风险判定和脱敏替换的代码逻辑。
  3. 同步更新了核心模块文档 `docs/03_核心模块说明.md`。
- **原因**：剧透风险判定目前已无必要，移除可提升系统的灵活性，并彻底消除问答/续写时的不必要拒答或打码隐藏。
- **验证**：运行了 `conda run -n chaishu python -m pytest`，测试套件全部 342 个用例全部 PASS 通过。

---

### 2026-05-24 fix 修复 Dashboard 模块脚本 MIME 类型

- **涉及文件**：`novel_system/api.py`, `tests/test_dashboard_book_actions.py`
- **变更内容**：在 FastAPI 应用挂载静态目录前注册 `.js` 为 `application/javascript`，并新增 Dashboard 模块脚本 MIME 回归测试；同时将书籍操作入口测试同步到当前模块化前端文件 `static/js/library.js`。
- **原因**：浏览器会拒绝以 `text/plain` 返回的 `<script type="module">`，导致 `main.js` 未执行，进而“导入新书”等按钮事件没有绑定。
- **验证**：先运行 `conda run -n chaishu python -m pytest tests/test_dashboard_book_actions.py -v` 观察到 MIME 断言失败；修复后该命令 2 个用例全部 PASS，并运行 `conda run -n chaishu python -m pytest`，343 个用例全部 PASS。

### 2026-05-25 refactor 重构并分拆 indexing 模块

- **涉及文件**：`novel_system/indexing/` 目录, `novel_system/indexing_legacy.py`, `novel_system/graph_name_policy.py`, `docs/01_目录结构说明.md`, `docs/03_核心模块说明.md`
- **变更内容**：
  1. 完成 Phase 4，在 `indexing_legacy.py` 中将 TF-IDF/向量和人名过滤/提取方法委托给 `vector_indexing.py` 和 `llm_extraction.py`。
  2. 完成 Phase 5，创建 `artifact_builders.py` 以纯函数承载所有制品构建，创建 `repository.py` 以存放 `BookIndexRepository`。
  3. 完成 Phase 6，更新 `__init__.py` 重新直接导出所有模块，打破了 `graph_name_policy.py` 中的循环依赖，且不依赖 legacy 桥接。
- **原因**：将庞大（~1700 行）且职责过重的 `novel_system/indexing.py` 彻底解耦为高内聚低耦合的子包结构，提升代码可维护性，同时对外暴露的 API 完全无改动。
- **验证**：运行 `conda run -n chaishu python -m pytest`，全量 343 个测试用例全部通过。

### 2026-05-24 fix 修复“导入新书”按钮无反应问题

- **涉及文件**：`static/js/library.js`, `static/js/main.js`, `templates/dashboard.html`
- **变更内容**：
  1. 重构了 `library.js` 中的书籍导入逻辑，将动态创建的文件 `input` 改为持久化挂载到 DOM 的隐藏元素，解决了浏览器安全策略拦截未挂载元素点击事件的问题。
  2. 在 `dashboard.html` 和 `main.js` 的脚本引用及模块导入中增加了版本号参数（`?v=2`），实现强行刷新浏览器缓存。
- **原因**：用户反馈点击导入按钮无反应，排查发现是浏览器拦截了代码触发的未挂载文件控件点击，且由于缓存问题导致旧代码持续运行。
- **验证**：人工测试按钮可正常弹出文件选择框；代码已同步。

### 2026-05-24 refactor/docs 测试套件精简与 conftest 抽离

- **涉及文件**：`tests/conftest.py`, `tests/test_vector_retrieval.py`, `tests/test_event_timeline.py`, `tests/test_semantic_scorer.py`, `novel_system/indexing/repository.py`
- **变更内容**：
  1. **新增** `tests/conftest.py`，将重复的 `AppConfig`、`MockEmbeddingProvider` 等 Fixture 集中管理，消除了各测试文件中的大量冗余初始化代码。
  2. **重构** `test_vector_retrieval.py`，彻底移除了对废弃 `HybridRetriever` 接口的依赖，改为测试 `SearchOrchestrator`，文件体积减少 60%。
  3. **修复** `repository.py` 中遗漏的 `score_event_sentence` 导入问题，解决了重构导致的 `NameError`。
  4. **清理** `test_event_timeline.py` 和 `test_semantic_scorer.py` 中的冗余模拟代码并适配新接口。
- **原因**：测试套件存在大量重复代码，且部分用例还在测试已废弃的旧接口，导致维护成本高且存在重构残留的 Bug。
- **验证**：运行 `conda run -n chaishu python -m pytest`，全量 343 个测试用例全部 PASS 通过。

---

### 2026-05-25 chore 初始化 GraphRAG 迁移 PRD 任务

- **涉及文件**：`.claude/skills/ralph/prd.json`, `.agents/skills/ralph/prd.json`, `.claude/skills/ralph/progress.txt`, `.agents/skills/ralph/progress.txt`
- **变更内容**：
  1. 归档了已完成的前端重命名/明亮模式重构任务（`ralph/frontend-light-professional-redesign`）至归档目录。
  2. 重置了 `.claude` 与 `.agents` 下的 Ralph 任务进度日志。
  3. 将 `2026-05-25-graphrag-migration-design.md` 中拆分出来的 11 个 Phase 精细解耦为 23 个微型 User Story，并写入 Ralph 的 `prd.json` 任务池。
- **原因**：为启动 GraphRAG 架构迁移任务做前置准备，使 Agent 能够以极小颗粒度、可自验证的形式推进开发，规避上下文溢出和红线违规。
- **验证**：手工检查 `prd.json` 语法无误。

---


## 待确认

- 待确认：`docs/07_冗余文件清理记录.md` 是否需要补建。
- 待确认：已删除评测链路是否代表项目正式放弃离线完整评测和检索评测工具。
- 待确认：`scripts/`、`tasks/`、`data/eval/`、`data/eval_full/` 空目录是否保留。
- 待确认：项目级 `.agents/skills/**`、`.claude/skills/**` 是否仍是当前协作流程必需。
- 待确认：`README.md`、`docs/02_技术栈与启动流程.md`、`AGENTS.md` 中启动命令是否需要统一。
### 2026-05-25 fix 完善 GraphRAG 测试体系并修复投影/解析问题

- **涉及文件**：`tests/graphrag_app/*.py`, `tests/test_validation_confidence.py`, `novel_system/graphrag_app/graph_projector.py`, `novel_system/indexing/parser.py`, `docs/09_变更记录.md`
- **变更内容**：补强 settings/input/table loader/graph/timeline/query router/answer adapter/API ask 的真实契约测试；验证 SpoilerGuard 已不在活跃 validator API；修复 graph projector 的实体端点别名解析和悬空边过滤；为章节解析增加真实中文“第X章”fallback。
- **原因**：原 GraphRAG 测试多为实例化或字符串级断言，无法覆盖真实文件产物、parquet 读取、API 契约和投影边界问题。
- **验证**：运行 `conda run -n chaishu python -m pytest --collect-only -q`，收集 181 个活跃测试；运行 `conda run -n chaishu python -m pytest -q`，181 passed，3 warnings。

---

### 2026-05-25 delete/chore 物理删除 legacy 目录并验证 GraphRAG 全流程索引

- **涉及文件**：`novel_system/legacy/` 目录, `tests/legacy/` 目录, `data/books/small-test/` 目录, `novel_system/indexing/repository.py`
- **变更内容**：
  1. 物理删除了隔离的 legacy 代码和测试目录。
  2. 修复了由于物理删除 legacy 文件夹导致 `novel_system/indexing/repository.py` 抛出的 `ModuleNotFoundError: No module named 'novel_system.legacy'` 错误。
  3. 清空了 `small-test` 书籍的原索引缓存及输出，通过 REST API `POST /api/books/small-test/start-index?force=true` 强制重新启动 GraphRAG 索引构建流程。
  4. 监视并验证了 GraphRAG 的 entity extraction、covariates extraction、community reports 以及 final embedding 流程全部跑通，成功生成了 `communities.parquet`, `community_reports.parquet`, `covariates.parquet`, `documents.parquet`, `entities.parquet`, `relationships.parquet`, `text_units.parquet` 等全部 parquet 表文件。
- **原因**：响应用户指令物理清除遗留代码，消除系统内老旧流程；同时重跑 GraphRAG 索引管道以全链路验证构建流程的可靠性。
- **验证**：运行 `conda run -n chaishu python -m pytest`，全量 181 个测试全部 PASS 通过；`GET /api/books/small-test/status` 接口返回 100% 进度且状态为 ready。

---

### 2026-05-25 docs 全面同步更新以对齐代码现状

- **涉及文件**：`docs/00_项目总览.md`, `docs/01_目录结构说明.md`, `docs/02_技术栈与启动流程.md`, `docs/06_项目接管审查报告.md`, `docs/10_待办与风险.md`, `docs/12_上下文恢复指南.md`, `docs/09_变更记录.md`
- **变更内容**：
  1. **架构同步**：在 `docs/12` 中重构一句话架构、问答链路答复以及数据存储形式为 GraphRAG 模式，并将 `indexing.py` 说明升级为已拆分重构的 `indexing/` 包。
  2. **前端模块化对齐**：将 `static/app.js` 标记为废弃死代码，修改已知风险、技术栈、审查报告 and 待办，声明前端 ESM 模块化重构已全部完成，明确指出 `static/app.js` 仅为兼容 `test_dashboard_graph_density.py` 测试而保留。
  3. **冗余项审计扩展**：在 `docs/01` 和 `docs/10` 标记 `novel_system/search/`、`novel_system/vector_store/` 和 `retrieval.py` 为待物理清理的冗余包/文件，确认根目录 `诛仙` 文件副本已被复制导入到 `data/uploads/` 中。
  4. **API 路由补全**：在 `docs/02` 技术栈中将原本只写了 6 个的核心路由表完整补齐为全部 20 个活跃的 FastAPI 端点路由。
  5. **测试数据同步**：在 `docs/01` 中将测试套件用例数描述从 350+ 同步更新为实际的 181。
- **原因**：修正文档与代码库在 GraphRAG 迁移及前端重构后存在的严重滞后和描述偏差，确保开发上下文完全一致，杜绝 AI 开发时受旧指南误导。
- **验证**：人工核对文档，并运行 `conda run -n chaishu python -m pytest` 验证全量 181 个测试均通过。

---

### 2026-05-25 docs 重写 README.md 以对齐项目真实状态

- **涉及文件**：`README.md`, `docs/09_变更记录.md`
- **变更内容**：
  1. 重构并重写了根目录下的 `README.md`，真实反映当前 GraphRAG 架构状态，标注旧 RAG 模块已被彻底物理清理。
  2. 修正已失效的启动和索引脚本命令（如 `scripts/build_index.py`, `scripts/run_api.py` 等已彻底不存在的遗留脚本），统一规范为真实的 `start_server.py` 和 API 触发路径。
  3. 补充了“快速开始”与详细的本地 Embedding / 大模型环境变量配置指南。
  4. 补充了常见排错机制，包含前端缓存强刷、索引报错排查、环境依赖等。
- **原因**：解决 README 中的失效启动方式、新旧架构混杂问题，帮助使用者快速上手、进行配置和排错。
- **验证**：人工检查 README 结构无误，已失效的 scripts 运行说明被彻底纠正；运行 `conda run -n chaishu python -m pytest` 验证全量 181 个测试全部 PASS。

---

### 2026-05-25 feat 细化 GraphRAG 索引进度显示

- **涉及文件**：`novel_system/services/indexing.py`, `tests/test_graphrag_index_progress.py`, `docs/09_变更记录.md`
- **变更内容**：
  1. 在书籍状态接口中读取 GraphRAG `indexing-engine.log`，解析 `extract graph`、实体关系描述汇总、事件线索抽取、社区报告和文本向量等内部阶段进度。
  2. 将 GraphRAG CLI 黑盒阶段映射到 30% 到 85% 的实时进度区间，使前端现有轮询可以显示更细颗粒度的百分比和阶段文案。
  3. 新增回归测试覆盖实体关系抽取和实体关系描述汇总两类日志进度解析。
- **原因**：GraphRAG CLI 阶段耗时较长，原实现只在进入 CLI 前显示 30%，CLI 结束后才跳到 85%，用户无法判断任务是否仍在推进。
- **验证**：先运行 `conda run -n chaishu python -m pytest tests/test_graphrag_index_progress.py -v`，新增 2 个测试按预期失败；实现后同命令 2 个测试 PASS。随后运行 `conda run -n chaishu python -m pytest`，全量 183 个测试全部 PASS。

---

### 2026-05-26 feat 在 LLM API 交互层增加请求与响应日志埋点

- **涉及文件**：`novel_system/llm.py`
- **变更内容**：
  1. 在 `llm.py` 中引入 `json` 模块。
  2. 在 `MiniMaxDirectClient.chat` 方法中，在调用 `requests.post` 发送请求前记录输入的消息列表、模型、温度及最大 Token 数，并在获取响应前记录响应结果和 Token 使用量。
  3. 在 `MimoClient.chat` 方法中，同样在发送 API 请求前以 JSON 格式记录 Payload，并在返回响应前记录响应结果和规范化的 Token 使用量。
- **原因**：支持在后台（如 FastAPI 的后台控制台与日志系统）直观地观察和监控与大模型 API 交互时所发送和接收的具体内容。
- **验证**：运行了 `conda run -n chaishu python -m pytest tests/test_mimo_client.py -v` 验证客户端交互和数据转换逻辑正确；运行了 `conda run -n chaishu python -m pytest`，全量测试用例全部 PASS 通过。

### 2026-05-26 fix 修复 GB18030 小说上传导入失败

- **涉及文件**：`novel_system/indexing/repository.py`, `tests/test_import_text_encoding.py`, `docs/09_变更记录.md`
- **变更内容**：`prepare_chapters()` 读取源文本时先使用 UTF-8/UTF-8 BOM，遇到 `UnicodeDecodeError` 后回退到 `gb18030`；新增 GB18030 中文小说分章导入回归测试。
- **原因**：部分中文 TXT 小说为 GB2312/GBK/GB18030 编码，原逻辑固定按 UTF-8 读取，上传后会在分章阶段抛出 `UnicodeDecodeError` 并导致 `/api/books` 返回 500。
- **验证**：先运行 `conda run -n chaishu python -m pytest tests/test_import_text_encoding.py -v` 观察到新增测试因 `UnicodeDecodeError` 失败；修复后该命令 1 passed；随后运行 `conda run -n chaishu python -m pytest`，结果为 131 passed, 2 warnings。

---

### 2026-05-27 fix/feat 修复诛仙章节解析并支持限制 GraphRAG 索引切片数

- **涉及文件**：`novel_system/indexing/parser.py`, `novel_system/graphrag_app/input_builder.py`, `tests/graphrag_app/test_input_builder.py`, `docs/09_变更记录.md`
- **变更内容**：
  1. **修复解析 Bug**：重写了 `parser.py` 中的 `parse_chapters()` 函数，改用合并并排序 `CHAPTER_RE` (阿拉伯数字) 和 `CHINESE_CHAPTER_RE` (中文数字) 的匹配项，解决了当小说中混有极个别阿拉伯数字章节标题时（例如诛仙的“第3章 ~依偎~”）导致其他 238 个中文数字标题被完全忽略的问题。
  2. **支持索引切片限制**：在 `input_builder.py` 中引入 `GRAPHRAG_MAX_CHAPTERS` 环境变量，允许限制写入 GraphRAG 输入目录的章节切片数量，以在开发调试时有效降低大模型 Token 消耗并缩短索引时间。
  3. **丰富测试套件**：在 `test_input_builder.py` 中新增 `test_build_from_txt_limits_chapters` 回归测试，确保环境变能够正确限制切片文件生成。
  4. **成功索引诛仙**：利用新机制限制索引前 5 章，配合 caching 机制成功重跑并构建了全套 GraphRAG Parquet 表，并生成了 `chapters.json` 使前端可流畅呈现所有 239 章列表，问答测试返回带正确引用的结构化回答。
- **原因**：用户反馈“用这个 RAG 帮我做一下文件夹里的诛仙”。排查发现之前因解析 bug 导致仅索引了 1 章；且长篇小说全量索引极其昂贵和缓慢（单章达 3 小时），因此引入了开发切片限制并重跑，彻底修复该问题。
- **验证**：运行 `conda run -n chaishu python -m pytest`，132 passed。运行测试问答脚本，成功获取正确的 RAG 检索回答。
