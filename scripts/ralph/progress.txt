# Ralph Progress

Branch: ralph/vector-search-integration
Started: 2026-04-28

## Status
- [x] US-001: 在 _run_book_index 中添加 FAISS 向量索引构建
- [ ] US-002: 更新 manifest 记录向量索引状态
- [ ] US-003: 验证 embedding_provider 正确传入
- [ ] US-004: 添加向量索引集成测试
- [ ] US-005: 更新模块文档说明向量索引构建

## Notes
初始创建 PRD，准备开始执行。

## Codebase Patterns
- 向量索引路径统一使用 `book_dir / "vectors"` 而非哈希路径，确保 build_from_txt()、_run_book_index() 和 load() 路径一致
- 使用 `corpus_vector_dir.mkdir(parents=True, exist_ok=True)` 确保目录存在后再保存

---

## 2026-04-28 20:50 - US-001
- 在 `_run_book_index()` 方法中添加了 FAISS 向量索引构建逻辑
- 为每个非空 corpus 调用 `_build_faiss_index()`
- 向量索引保存到 `book_dir/vectors/<corpus_name>/` 目录
- 捕获并记录向量索引构建异常，不影响其他 corpus 的索引
- 在 manifest 中添加了 `has_vector_index` 字段
- 修复了 `build_from_txt()` 和 `load()` 中向量路径不一致的问题
- 更新了 `_run_book_index()` 的 docstring

**Files changed:**
- `novel_system/service.py` - 添加向量索引构建逻辑和 manifest 字段
- `novel_system/indexing.py` - 修复路径一致性，添加目录创建

**Learnings for future iterations:**
- 向量索引路径必须保持一致：`build_from_txt()`、`_run_book_index()` 和 `load()` 都使用 `book_dir / "vectors"`
- 测试 `test_vector_retrieval.py` 验证了向量索引的构建、加载和持久化
- `embedding_provider` 通过 `BookIndexRepository` 构造函数传入，在 `NovelSystemService` 初始化时创建

---

